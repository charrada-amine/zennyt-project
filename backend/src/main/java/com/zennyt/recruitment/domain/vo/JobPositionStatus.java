package com.zennyt.recruitment.domain.vo;

/** Statut d'un métier dans le référentiel — voir {@link com.zennyt.recruitment.domain.model.JobPosition}. */
public enum JobPositionStatus {
    APPROVED, PENDING_APPROVAL, REJECTED
}
