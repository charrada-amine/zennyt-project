meta {
  name: 02 Me
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Identité portée par le jeton (le backend résout sub → utilisateur).
  200 avec le profil, 401 si jeton absent/expiré.
}
