meta {
  name: 23 Fit-score callback (AI)
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/api/v1/callbacks/fit-score
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Callback-Secret: {{callbackSecret}}
}

body:json {
  {
    "candidateId": "{{candidateId}}",
    "jobOfferId": "{{demoOfferId}}",
    "score": 91
  }
}

docs {
  Simule le service IA qui calcule le score de compatibilité (endpoint callback).
}
