meta {
  name: 00c Pending job positions (ADMIN)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/job-positions/pending
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

assert {
  res.status: eq 200
}

docs {
  File d'attente d'approbation — nouveau @AdminOnly (20/07), même mécanisme
  que @RecruiterOnly. Doit contenir la proposition de l'étape précédente.
}
